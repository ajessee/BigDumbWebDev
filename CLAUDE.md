# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

BigDumbWebDev (`www.bigdumbweb.dev`) is Andre's personal portfolio and blog site — a Rails 8.1.3.1 app (Ruby 3.3.9) built to document his own path into web development. It's deployed on Heroku (`big-dumb-web-dev`) and deliberately hand-rolled: vanilla JS/CSS rather than a frontend framework, a custom rich-text editor extension, a custom modal/notification/menu system, and a from-scratch auth system rather than Devise/Sorcery.

**The Rails 6.1 → 8.1 upgrade (Phase 1) is deployed to production as of 2026-09-15** (Sections 1-5 of the plan all done; see [docs/archive/HEROKU-DEPLOYMENT.md](docs/archive/HEROKU-DEPLOYMENT.md) for the rollout record, including a real production migration bug found and fixed during that deploy). For historical detail on exactly what was outdated, broken, or risky before this upgrade and how each thing was fixed, read the archived [docs/archive/UPGRADE-PLAN.md](docs/archive/UPGRADE-PLAN.md) and [docs/archive/UPGRADE-LEARNINGS.md](docs/archive/UPGRADE-LEARNINGS.md) (lessons carried over from the sibling RDJesseeBlog app's completed identical upgrade). For current, forward-looking work (Phase 2/3), read [docs/development/ROADMAP.md](docs/development/ROADMAP.md) first. For anything touching DNS, domain registration, or email deliverability, read [docs/development/DOMAIN-AND-EMAIL.md](docs/development/DOMAIN-AND-EMAIL.md). For backups/restore, read [docs/development/BACKUP-AND-RESTORE.md](docs/development/BACKUP-AND-RESTORE.md). Deploys now auto-migrate via a Heroku release phase (`Procfile`) — see [docs/development/PRODUCTION-DOCKER.md](docs/development/PRODUCTION-DOCKER.md).

## Commands

Local dev runs in Docker Compose (`docker-compose.yml`/`Dockerfile.dev` — see [docs/archive/UPGRADE-PLAN.md](docs/archive/UPGRADE-PLAN.md), not the standalone Ruby toolchain the commands below might suggest): `docker compose up -d db app shakapacker`, project name `bigdumbwebdev_dev`. A separate production Dockerfile (`Dockerfile`, no `.dev`) exists for portability validation only — see [PRODUCTION-DOCKER.md](docs/development/PRODUCTION-DOCKER.md); production itself still deploys via Heroku buildpacks.

- Setup: `bin/setup` (bundles, runs `bin/yarn`, `bin/rails db:prepare`, clears logs/tmp, restarts) — or just `docker compose up -d db app shakapacker` for the Docker dev environment, which handles this itself.
- Dev server: `bin/rails server` — **also requires `bin/shakapacker-dev-server` running in a separate process** for JS/CSS to recompile (standard Shakapacker dual-process setup; the `shakapacker` Compose service already runs this).
- All non-system tests: `bin/rails test` (current bar: 97/186 assertions, 0 failures/errors — see [docs/archive/UPGRADE-PLAN.md](docs/archive/UPGRADE-PLAN.md) before assuming this number is stale)
- System tests only (not run by `bin/rails test`): `bin/rails test:system` — headless Chrome via Selenium 4 (current bar: 37/749 assertions, 0 failures/errors)
- Single test file: `bin/rails test test/models/user_test.rb`
- Single test by name: `bin/rails test test/models/user_test.rb -n test_method_name` (or `-n "/pattern/"`)
- Lint: `bundle exec rubocop` (no `.rubocop.yml` — runs on RuboCop's default rules; the Gemfile comment aspires to a pre-commit hook, but none is actually installed in `.git/hooks`)
- Security scan: `bin/security-scan` (Brakeman + `bundler-audit` + `yarn audit`; see [SECURITY-ACCEPTANCE.md](docs/development/SECURITY-ACCEPTANCE.md) for currently-accepted, tracked findings before treating any script output as a new regression)
- Container image scan: `bin/container-security-scan [image]` (Docker Scout; same acceptance doc applies)
- Pull production data into development: `rake db:pull_prod_db` — captures a real Heroku Postgres backup and restores it over the local dev database. Local dev now defaults `config.active_storage.service` to `:local` (Disk), not `:amazon`/S3 like production — set `ACTIVE_STORAGE_SERVICE=amazon` explicitly if you actually want pulled-prod media to resolve after using this task.
- Regenerate `test/fixtures/*.yml` from the current dev database: `rake db:create_fixtures`.
- **Deploy:** `git push heroku main:master` — GitHub's default branch was renamed `master` → `main` on 2026-09-15, but Heroku's git remote (a separate protocol, not tied to GitHub at all) still only accepts pushes to a ref literally named `master`; that has nothing to do with GitHub's default-branch setting and doesn't need to change. Migrations now run automatically as a Heroku release-phase step (`Procfile`'s `release:` line) — no separate `heroku run rails db:migrate` needed after a normal deploy.

## Architecture

**Authorization is entirely controller-level, not route-level.** `config/routes.rb` has no `constraints`/`namespace` scoping by role — every auth/ownership check lives in `before_action` callbacks inside each controller (e.g. `UsersController`'s `logged_in_user`/`correct_user`/`admin_user`). When adding a new mutating action, the existing pattern to follow is: add it to the relevant `before_action ..., only: %i[...]` list(s), not just implement the action. This same pattern is *why* `remove_resume` was missing its guard until recently — it's easy to add an action and forget to also add it to every relevant filter's `only:` list.

**Two-stage anonymous "guest" identity system**, distinct from full signup: `User#role` enum is `guest_1 → guest_2 → user → admin` (`enum :role, %i[guest_1 guest_2 user admin]`, the modern positional syntax — migrated from the old keyword-arg form as part of the Rails 8.0 step, since 8.0 removed the old syntax outright rather than just deprecating it; same integer mapping preserved). `GuestUsersHelper` creates a cookie-identified `guest_1` user on first visit (random `guest_#{uuid}@bigdumbweb.dev` email, shared password from Rails credentials) so anonymous visitors can comment; `UsersController#create` promotes `guest_1 → guest_2` when someone starts filling out the signup form, and `GET /demote_guest` can reverse that. All of `ApplicationController`'s cross-cutting behavior (`SessionsHelper`, `GuestUsersHelper`, `MessagesAndCookieHelper`, `ErrorsHelper`, `PostsHelper`) is mixed in via `include`, not inheritance — start there when tracing where a helper method used across controllers is actually defined.

**Rich text (Action Text/Trix) has a custom, DOM-coupled toolbar extension.** `User#details`, `Post#content`, and `Comment#content` all `has_rich_text`. `app/javascript/application/trix.js` registers custom heading levels, foreground/background color swatches, underline, a horizontal-rule attachment, and an "attach files" action — all via direct `Trix.config`/DOM manipulation against Trix's internal toolbar structure, so a Trix version bump needs a manual visual smoke test, not just a green test suite. `config/initializers/action_text.rb` allow-lists the `style` HTML attribute specifically so the color-swatch feature survives Action Text's sanitizer — if that allow-list is ever lost, existing colored/heading rich text will silently render unstyled rather than error. Code syntax highlighting (Prism) is a CDN script tag in the layout (`app/views/layouts/_prism_js.html.erb`/`_prism_css.html.erb`), not an npm package — it's independent of the Rails/Trix/bundler stack entirely.

**Frontend JS has an implicit load-order dependency, not a module system.** Everything under `app/javascript/application/` is loaded together via one Shakapacker (formerly Webpacker) entrypoint (`packs/application.js`), via `app/javascript/application/index.js`'s webpack-only `require.context` call; `main.js` sets global `window.utils`/`window.projects` objects that other files (e.g. `projects-3d-scroll.js`, the 3D portfolio effect) read as globals. This is exactly why Webpacker's replacement stayed on a real webpack-based tool (Shakapacker) rather than an import-map/esbuild swap — `require.context` and the implicit-global pattern only work under webpack. A future move to import maps (Phase 2) needs to either preserve load order explicitly or make these dependencies explicit.

**Active Storage**: `User` has `image`/`resume` (`has_one_attached`), `Project`/`Resource` have `image`. No custom variants/previews are defined anywhere — attachments are used as-is (`variant_processor = :mini_magick`, not the Rails 7.1+ `:vips` default — see the archived UPGRADE-PLAN.md's `image_processing` finding for why). Storage service is S3 (`:amazon`) in production; development defaults to local disk (`:local`) since Phase 1, overridable with `ACTIVE_STORAGE_SERVICE=amazon` (see the `pull_prod_db` note above).
