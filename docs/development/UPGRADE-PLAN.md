# BigDumbWebDev upgrade plan

Updated September 12, 2026. Status: no upgrade steps have been taken yet. This plan was fleshed out from a full source-level audit (Gemfile/Gemfile.lock, package.json/both lockfiles, `config/routes.rb`, every controller and model, the test suite, and deployment config) rather than the September 9 external assessment alone — every claim below was verified by reading the actual code, not inferred from RDJesseeBlog's experience.

Implementation handoff: [RDJesseeBlog upgrade lessons and repeatable roadmap](UPGRADE-LEARNINGS.md). Read this before implementation — RDJesseeBlog's own upgrade is now complete and deployed, so treat its plan/roadmap as a finished reference, not a work-in-progress. This upgrade is Phase 1 of a three-phase plan — see [ROADMAP.md](ROADMAP.md) for what comes after it (adopting Rails 8+ frontend capabilities, then a UI/UX redesign).

## Scope and deployment approach

Upgrade BigDumbWebDev and RDJesseeBlog as separate Rails applications, keep both on Heroku initially, add tested Docker configurations, improve security and operations, and investigate costs. Preserve this site's blog, portfolio, editor, account and comment workflows, visual identity, and URLs. RDJesseeBlog's static conversion is a decision for that app alone; there is no static conversion planned for BigDumbWebDev.

Working recommendation, matching RDJesseeBlog: retain Heroku buildpacks on the latest supported numbered stack while adding Docker for reproducible development and portability. Direct Docker deployment on Heroku is optional, uses the `container` stack, and does not eliminate dyno or database charges.

## Verified baseline

| Area | Finding |
| --- | --- |
| Ruby / Rails | `.ruby-version` and `Gemfile` pin Ruby `3.0.0`; `Gemfile.lock` resolves Rails to `6.1.3`. **`config/application.rb` still has `config.load_defaults 6.0`** — the app has never actually adopted Rails 6.1 framework defaults despite running the 6.1 gem, so the 6.0→6.1 defaults migration is its own unstarted step, separate from any gem-version bump. Bundler pinned at `2.2.6`. |
| Heroku app | `big-dumb-web-dev`, US region, one Basic web dyno, stack `heroku-20` (verified via `heroku stack`; `heroku-22/24/26` are all available to move to). Current release `v281` (Feb 2026) is a database-config-only bump — no application code has shipped in a long time. Buildpacks: `heroku-buildpack-activestorage-preview` + `heroku/ruby`, **no explicit Node buildpack** (Heroku's classic Ruby buildpack auto-provisions Node/Yarn when it detects Webpacker; this stops working once Webpacker is removed, so a `heroku/nodejs` buildpack must be added explicitly at that point, same as RDJesseeBlog needed). |
| Add-ons | `heroku-postgresql:essential-0` (~$5/mo cap), `pointdns:developer` (free), `sendgrid:starter` (free) — verified via `heroku addons`. Config vars present: `DATABASE_URL`, `RAILS_MASTER_KEY`, `RAILS_LOG_TO_STDOUT`, `RAILS_SERVE_STATIC_FILES`, `RAILS_MAX_THREADS`, `SENDGRID_*`. **No `BIG_DUMB_WEB_DEV_DATABASE_PASSWORD` var exists**, even though `config/database.yml`'s `production:` block is written to use explicit `database`/`username`/`password` fields (sourced from that env var) rather than `DATABASE_URL` directly — needs reconciling before assuming either path is what's actually in effect today. |
| JS/asset tooling | Both `package-lock.json` (8,496 lines) and `yarn.lock` (6,835 lines) exist and currently agree with each other, but having both violates "pick one package manager." `package.json`'s `@rails/webpacker` dependency is **git-sourced directly from `github.com/rails/webpacker`**, not from the npm registry — a hard blocker for any `npm ci`/`yarn install --frozen-lockfile` on a locked-down build image. `@rails/actioncable`/`@rails/activestorage`/`@rails/ujs` are still on leftover `^6.0.0-alpha`/`^6.0.0-beta1` ranges. `.nvmrc` pins **Node 12.19.0** (EOL, incompatible with any modern bundler that would replace Webpacker). |
| Frontend | ERB, Sprockets/Sass (`sass-rails` 6.0.0 → `sassc` 2.4.0, a native-extension gem), Webpacker 5.2.1, custom vanilla JS (no jQuery). 27 files under `app/javascript/application/`, all loaded together via one Webpacker entrypoint with implicit load-order dependencies (`main.js` sets `window.utils`/`window.projects` globals other files read). Includes a 1.1 MB vendored `fontawesome-all.min.js`, a custom Trix toolbar extension (`trix.js`), the 3D-portfolio scroll effect (`projects-3d-scroll.js`), the animated landing page (`fadeIn.js`), and the local-storage blog autosave (`postAutoSaver.js`). |
| Action Text / Trix | `actiontext` gem locked to exactly `6.1.3`; npm `trix@1.3.0` / `@rails/actiontext@6.0.3` (resolved from loose ranges in `package.json`). Three models use rich text: `User#details`, `Post#content`, `Comment#content`. A custom `config/initializers/action_text.rb` allow-lists the `style` HTML attribute so the custom Trix color-swatch/heading extensions survive sanitization — **this must be re-verified after the Rails/rails-html-sanitizer/loofah upgrade**, since a lost allow-list would silently un-style every existing colored/heading rich-text record. Prism (code syntax highlighting) is **CDN-loaded** (`cdnjs.cloudflare.com`, pinned to Prism **1.17.1**, from 2019), not npm-managed — untouched by the Webpacker migration but worth upgrading and worth allow-listing explicitly if a CSP is ever added (see below). |
| Active Storage | `db/schema.rb` (version `20210305030844`) and `db/migrate/20210305030844_create_active_storage_variant_records.active_storage.rb` are present and consistent with each other — **unlike RDJesseeBlog, this app does not have the "missing migration" gap** that broke image serving there under Rails 8.1's `VariantWithRecord`. Still worth a `db:migrate:status` check against the live app before/after the eventual cutover rather than trusting this source-level read alone. `config.active_storage.service` is `:amazon` in **both development and production** — local development is configured to hit real S3 by default (a deliberate historical choice, per an inline comment, to work against production-pulled data), which needs an explicit decision before Docker/local-dev work begins so a fresh dev environment doesn't need real AWS credentials just to boot. |
| Auth / roles | Custom `has_secure_password`, no Devise/Sorcery. `User#role` uses the **old keyword-arg `enum role: [...]` syntax** (`guest_1=0, guest_2=1, user=2, admin=3` implicitly) — needs migrating to the modern positional `enum :role, [...]` form while preserving the exact same integer mapping. A two-stage anonymous-guest system (`guest_1`/`guest_2`) backs anonymous commenting before signup. |
| Tests | Minitest (not RSpec) confirmed. **A file existing is not evidence of coverage here** — counted live (non-commented) `test` blocks directly: models have 18 real assertions (`user`: 13, `post`: 5); `Comment`/`Project`/`Resource`/`Tag`/`Tagging` model tests are empty stubs (0). Controllers have 7 real assertions total (`errors`: 5, `sessions`: 1, `tags`: 1); **`users_controller_test.rb`, `projects_controller_test.rb`, and `hello_controller_test.rb` are 100% commented out** (0 live tests each) despite existing as files; no test file exists at all for `PostsController` or `CommentsController` — the two controllers with the most business logic, and the one (`Comments`) with a confirmed authorization gap (see below). The one integration test file (`user_edit_test.rb`) is also 100% commented out. **Total live, non-system-test assertions in the whole app: 25.** The only substantial real coverage is 6 Capybara system tests, but they run non-headless Chrome via `selenium-webdriver 3.142.7` + the deprecated `webdrivers` 4.6.0 gem and can't run in CI/Docker until that driver is replaced — meaning there is currently no coverage runnable in an automated environment at all. See the dedicated Testing strategy section below rather than treating "run the suite" as sufficient. |
| Deployment | No Dockerfile, Compose config, or CI workflow exists. `Procfile` is already present and correct (`web: bundle exec puma -C config/puma.rb`) — one less thing to add versus RDJesseeBlog, which had to create this. |
| Content/domain | Portfolio + blog site (posts, projects, tags, nested comments), `www.bigdumbweb.dev`, automatic cert management. No audio/video processing (no ffmpeg dependency needed, unlike RDJesseeBlog) — image-only media via `has_one_attached :image` (User, Project, Resource) and `:resume` (User). |

## Known issues found during the code audit

These were confirmed by reading the actual source, not inferred — treat them as findings to act on, not hypotheses to re-verify.

**Security — fix independently of the Rails upgrade, not gated on it:**

- **`GET /remove_user_resume/:id` (`UsersController#remove_resume`) has no authentication or ownership check at all.** Its sibling `remove_image` has both `logged_in_user` and `correct_user` guards in its `before_action :only` list; `remove_resume` was left out of both lists entirely (`app/controllers/users_controller.rb:108-115`). Any anonymous visitor can hit this URL for any user ID and permanently detach that user's resume, right now, in production. This looks like an accidental omission (the sibling action does it correctly) rather than a design choice. Recommend fixing this as its own small, immediate change — it doesn't need to wait for the Rails upgrade.
- `GET /remove_user_image/:id` and `GET /demote_guest` are both state-changing GET routes (correctly guarded on auth where relevant, but GET-for-mutation is itself bad practice — Rails 8.1's per-form CSRF/Origin-validation defaults are a natural point to convert these to `DELETE`/`PATCH`).
- `CommentsController#create`'s strong params (`comment_params`) permit a client-supplied `:user_id`, and nothing overwrites it for a logged-in user before that params hash is built (only the guest path substitutes `guest_user.id`). A logged-in user can currently submit an arbitrary `user_id` in the comment form and have the comment attributed to someone else.
- CSP is fully disabled (`config/initializers/content_security_policy.rb` is the untouched, all-commented-out Rails template). Low urgency today since nothing else assumes a CSP exists, but worth closing once Prism/Font Awesome/any other CDN or vendored asset sourcing is finalized.

**Logic bugs, independent of any framework version:**

- `TagsController#destroy` iterates `@posts.each { |post| post.tags.destroy(@tag) }`, redundantly destroying the same `@tag` object on every iteration of the loop instead of once — worth a second look regardless of the upgrade.
- `User#fetch_ip` makes a live outbound HTTP call to `http://checkip.amazonaws.com/` on every non-production request, and `UsersController#show` calls it unconditionally — this fires on every dev-server page load and every test/system test that visits a user page, and needs stubbing before a real Docker/CI baseline is useful.
- **`Geocoder`/`User#guess_city/guess_region/guess_country/guess_address` are *not* dead code** (corrected 2026-09-14 — this section previously claimed no callers existed). `app/views/users/_profile.html.erb` calls all three on every own-profile view. No lookup is configured, so it hits a live geocoding API in dev/production; more importantly, `guess_city` etc. crash outright (`NoMethodError` on `nil.city`) whenever the IP can't be geocoded — a nil `ip_address` (a user who's never had `fetch_ip` called on them) or a private/loopback address both hit this. Stubbed in `config/environments/test.rb` only; **TODO: guard `guess_city`/`guess_region`/`guess_country`/`guess_address` against a nil `Geocoder.search` result** (or nil `ip_address` directly) before this bites a real user's profile page. Not yet fixed — flagged, not actioned, while the version climb is in progress.
- Two orphaned tables, `versions` and `version_associations`, exist in `db/schema.rb` with no corresponding migration and no `paper_trail`-style gem anywhere in the Gemfile — likely leftovers from a removed auditing gem. Decide whether to drop them via a new migration or just document them as intentionally orphaned.

**Dependency/tooling landmines to expect, based on RDJesseeBlog's experience:**

- `mimemagic 0.3.5` is a **transitive** dependency (via `activestorage 6.1.3` → `marcel 0.3.3`) — the exact yanked-from-RubyGems version that broke RDJesseeBlog's fresh installs. Expect the identical failure on a clean `bundle install`/Docker build here.
- `rubocop`, `brakeman`, `pry-byebug`, `faker`, and `humanize` all live in the Gemfile's **default group**, meaning they currently ship to production. Move each to `:development`/`:test` (faker likely only needed for `db/seeds.rb`).
- `selenium-webdriver 3.142.7` + `webdrivers 4.6.0` need replacing together — Selenium 4's built-in Selenium Manager obsoletes the `webdrivers` gem, and Selenium 3's protocol won't drive a current Chrome/Chromedriver pairing.
- `will_paginate` is currently `3.3.0`, not yet on the `4.x` line that broke RDJesseeBlog's Bootstrap pagination renderer — so that specific break hasn't happened yet here, but will need the same renderer check whenever this app's `will_paginate` gets bumped to 4.x.
- `sass-rails`/`sassc` (native extension) will need replacing alongside Webpacker, same as RDJesseeBlog's Sass/Bootstrap asset pipeline.

## Testing strategy

Current coverage is smaller and less capable than a file listing suggests (see the Tests row above: 25 live assertions total, and no coverage at all that can currently run in CI/Docker). "Run the existing suite and record failures" — RDJesseeBlog's actual first step — doesn't work as-is here, because most of what looks like test infrastructure will report 0 examples rather than failing ones. This section replaces that assumption with a sequenced plan, gated the way RDJesseeBlog's version climb was (build a real baseline *before* advancing, not after).

**Step 0 — make the suite runnable at all (blocking prework, not a Phase 2 nice-to-have):** DONE (2026-09-13/14).

- [x] Replace `selenium-webdriver 3.142.7` + the deprecated `webdrivers 4.6.0` gem with current Selenium 4 (built-in Selenium Manager). Nothing else below matters until system tests can run headlessly in Docker/CI — this is currently filed under Phase 2's general dependency modernization; treat it as a prerequisite instead.
- [x] Switch `ApplicationSystemTestCase` to a headless Chrome driver (it currently forces a visible window with devtools forced open, and hardcodes port 3001).
- [x] Resolve the missing fixture data — `test/fixtures/users.yml` doesn't exist despite being referenced by (dead) tests. Decide fixtures vs. a builder pattern now, rather than resurrecting broken references later. Decided fixtures; done in Step 1 below once needed for actual test content.

**Step 1 — establish the true baseline (don't trust a pass/fail count until you've checked what's actually running):** DONE.

- [x] Count live, executable assertions directly rather than trusting file presence (already done above: 18 model + 7 controller + 0 integration = 25, plus 6 system tests blocked on Step 0).
- [x] Decide, file by file, whether to resurrect the fully-commented-out test files (`users_controller_test.rb`, `projects_controller_test.rb`, `hello_controller_test.rb`, `user_edit_test.rb`) or replace them outright — don't just uncomment and hope, since some reference fixtures that don't exist. `user_edit_test.rb` deleted (superseded by the real, active `test/system/user_edit_test.rb` its own TODO called for); the other three resurrected, with real fixes where the original commented-out code no longer matched current behavior (stale `name:` param, a `login_as` helper that doesn't work in `ActionDispatch::IntegrationTest`). Added `test/fixtures/{users,posts,projects}.yml`, hand-written and synthetic rather than generated from a prod-synced dev DB.

**Step 2 — close the highest-value coverage gaps before touching any Rails version:** DONE.

- [x] `CommentsController` — no test file exists at all, and it has a confirmed authorization bug (trusted client `user_id`, see Known issues). Write the regression test alongside that fix, not after. Fixed and tested.
- [x] `PostsController` — no test file exists at all; this is the richest business logic in the app (`check_diffs`, admin-only actions, publish/draft visibility). Tested; also found and fixed a real bug surfaced by writing the `check_diffs` test (`payload[:allEmpty]` checked before the snake_case→camelCase key transform, so the "no differences" 204 branch was dead code).
- [x] A regression test for `remove_resume`'s corrected guard (the code fix already shipped; it's still untested — add the test once Step 0 makes a runnable environment possible). Added; also surfaced that `Geocoder` is *not* dead code as this doc previously claimed (`app/views/users/_profile.html.erb` calls `User#guess_city` et al on every own-profile view) — stubbed in `config/environments/test.rb` for now; the live-lookup fragility for a nil/private `ip_address` is still present outside test and is not yet fixed.
- [x] The five empty model stubs (`Comment`, `Project`, `Resource`, `Tag`, `Tagging`) — at minimum, validation and association coverage matching the depth already present for `User`/`Post`. Done.

**Step 3 — gate the version climb on a concrete number, not a vibe:** DONE.

- [x] Set an explicit completion bar before starting any Rails version bump — e.g., "every controller has at least one test per authorization boundary; zero fully-commented-out test files remain." RDJesseeBlog's equivalent gate was concrete and measured (82 → 107 passing examples before advancing past Rails 6.1); pick BDWD's number now rather than deciding "good enough" mid-upgrade. **Bar set 2026-09-14: 125 passing examples, 0 failures, 0 errors** (`bin/rails test`: 88 tests / 147 assertions; `bin/rails test:system`: 37 tests / 749 assertions), zero fully-commented-out test files remaining (the unmodified default `test/channels/application_cable/connection_test.rb` scaffold doesn't count as one of these - it was never part of this app's own test suite). This is the number to match or exceed after every Rails version step from here on.
- [ ] From then on, run the full suite (headless system tests included) after every Rails version step, same method RDJesseeBlog used — but this only provides real protection once Steps 0–2 are done. Running "the suite" today would silently pass while testing almost nothing.

## Implementation checklist

### 1. Reproduce and protect the existing app

- [x] Create an `andre/`-prefixed upgrade branch (matching the user's naming convention from RDJesseeBlog) and preserve local changes — done (`andre/bigdumbwebdev-upgrade`, all work this phase committed incrementally to it, nothing lost).
- [ ] Confirm the deployed Heroku source commit actually matches this checkout (release `v281` is a config-only bump, so the last real code release predates it — confirm which commit that is). Split out from the branch-creation item above since it's a separate, still-open check, not done as part of creating the branch.
- [ ] Arrange a fresh private database backup and a separate S3 media-inventory check; prove restoration before any production change. Exclude secrets/personal data from Git and Docker build contexts.
- [x] **Decide `config.active_storage.service` for local development explicitly — done.** `config/environments/development.rb` sets it to `ENV.fetch("ACTIVE_STORAGE_SERVICE", "local").to_sym` — defaults to the local Disk service so a fresh Docker container boots without needing real AWS credentials, while `ACTIVE_STORAGE_SERVICE=amazon` still opts back into pulled prod media when actually wanted.
- [x] **Boot locally with isolated Postgres and own Compose project/volume names — done** (`bigdumbwebdev_dev` project, `bigdumbwebdev_{pgdata,bundle,node_modules}` volumes, never collides with RDJesseeBlog's). Local media defaults to `:local` (see above) and dev mail uses `config.action_mailer.delivery_method = :test` (held in memory, never actually sent) — both already non-production-touching by default, not just by convention.
- [ ] **Still open:** `User#fetch_ip` still makes a live outbound call to `checkip.amazonaws.com` on every non-production, non-test request (`app/models/user.rb`) — it's stubbed to a fixed `127.0.0.1` in `test` only. Any dev request that triggers it (e.g. viewing a user profile) still hits a real external service.
- [x] Work through the Testing strategy section above (Steps 0–3) — done; see that section, all steps checked off, 125-example completion bar set 2026-09-14.
- [ ] Capture desktop/mobile examples of the 3D portfolio scroll, the animated landing page, the blog autosave, and Trix's custom toolbar (heading levels, color swatches, underline, horizontal rule, attach-files) — these are all hand-rolled and easy to silently break during the Webpacker replacement. Spot-checked ad hoc during the Webpacker → Shakapacker verification (landing animation, 3D scroll iframe, Trix-rendered post content, login modal all confirmed working in a real browser) but not formally captured as reference examples — this item is about the latter, still open.

### 2. Update Ruby, Rails and dependencies

(The `selenium-webdriver`/`webdrivers` replacement is Step 0 of the Testing strategy section above, done in Phase 1 — not listed again here since it's a prerequisite for testing, not a dependency-modernization nice-to-have.)

- [ ] Recheck current stable Rails/Ruby releases at implementation time (RDJesseeBlog's target was Rails 8.1.3.1 / Ruby 4.0.6 — don't assume those numbers are still current without checking).
- [x] First, flip `config.load_defaults 6.0` → `6.1` on its own and get the suite green — this is an unstarted step independent of any gem-version bump, since the app has been running Rails 6.1.3's gem with Rails 6.0 defaults the whole time. Done 2026-09-14: found and fixed a real regression this way, not just a formality — Rails 6.1 flips `form_with`'s default from remote (AJAX) to local, and six views (login, signup, user edit, comment new/edit, password reset request) relied on the old implicit `remote: true` for their JS-driven modal flow (their controllers only implement a `.js` response). Fixed with explicit `local: false` on each, rather than reverting the setting app-wide — see `config/initializers/new_framework_defaults_6_1.rb`. The unit/integration suite didn't catch this (it fakes the request format directly); only the headless *system* tests did, driving a real browser through the real form — exactly what Testing strategy Step 3 was for. Suite back to the 125-example bar (88 + 37, 0 failures/errors) after the fix; eager loading and both asset pipelines (`assets:precompile`, `webpacker:compile`) verified separately.
- [x] Then work through 6.1 → 7.0 → 7.1 → 7.2 → 8.0 → 8.1 (or newer stable), one version at a time, changing Ruby only at compatible steps, only after the Testing strategy section's Step 3 completion bar is met. Run the full suite (headless system tests included) + eager loading + asset compilation after every step. **All done, 2026-09-14 — the app runs on Rails 8.1.3.1 / Ruby 3.3.9.**
- [x] `config.load_defaults` bumped 6.1 → 7.0 → 7.1 → 7.2 → 8.0 → 8.1 to match the gem, done 2026-09-14, one version at a time with the same full-suite + eager-load + asset-pipeline verification as the gem-version steps. No historical `new_framework_defaults_X_Y.rb` template exists for the intermediate versions (the installed gem only ships its own current version's template), so each intermediate step relied on the same empirical bump-test-fix process already used for the gem bumps, rather than a pre-enumerated checklist — this is exactly what caught the `form_with` regression at the 6.0→6.1 step. **All five remaining steps (7.0 through 8.1) passed clean on the first try, zero regressions** — a real result, not a skipped check. Replaced the now-obsolete `config/initializers/new_framework_defaults_6_1.rb` with the authoritative `new_framework_defaults_8_1.rb`, copied verbatim from the installed Rails 8.1.3.1 gem: every setting in it is commented out, meaning every new 8.1-era default (JSON escaping changes, the relative-redirect `:raise` protection, etc.) is already accepted as verified, not merely defaulted-into unreviewed.
  - [x] **6.1 → 7.0 done 2026-09-14** (gem only; `config.load_defaults` stays `6.1` for now, to be bumped as its own deliberate step same as 6.0→6.1 was). Rails resolved to `7.0.10` (final 7.0.x release). Three real incompatibilities found and fixed, not just version-number churn:
    - `logger`/`mutex_m` (Ruby default gems) got pulled in as genuine `activesupport` dependencies under 7.0, not just via a test-only gem as before — same "already activated X, but Gemfile requires Y" class of conflict as Step 0's `logger` fix. Pinned both at top level (moved out of the `test` group) to match Ruby 3.0.7's bundled default versions.
    - **Spring (2.1.1, the latest release) is incompatible with Rails 7.0's Zeitwerk-only autoloading** (`ActiveSupport::Dependencies.mechanism=` no longer exists) — no newer Spring version exists to fix this. Removed entirely: dropped from the Gemfile, `bin/spring` deleted, `bin/rails`/`bin/rake` no longer load it.
    - Rails 7.0's file watcher requires `listen ~> 3.5`; the Gemfile's `< 3.2` ceiling was just the original `rails new`-generated default, not a deliberate pin — bumped.
    - Also needed a RubyGems update inside the dev image (3.2.33 → 3.4.22): Ruby 3.0.7's bundled RubyGems was too old to install `ffi`'s newer precompiled binary, pulled in once `listen`/dependencies moved.
    - Verified: `bin/rails test` 88/147, `test:system` 37/749, both 0 failures/errors (same 125-example bar); eager loading and both asset pipelines (`assets:precompile`, `webpacker:compile`) also verified clean.
  - [x] **7.0 → 7.1 done 2026-09-14.** Rails resolved to `7.1.6`. Four more real incompatibilities:
    - `psych` (Ruby default gem) got pulled to `5.5.0` transitively (via `irb`/`rdoc`), and Psych 5 changed `YAML.load`'s default to disallow aliases — broke `config/database.yml`'s `<<: *default` merge keys (`Psych::AliasesNotEnabled`). Pinned to `3.3.2`, matching Ruby 3.0.7's bundled default, same pattern as `logger`/`mutex_m`.
    - **`config/initializers/action_text.rb` broke exactly as flagged** — Rails 7.1 changed `ActionText::ContentHelper.allowed_attributes` to have no default (`nil`); the real allow-list is now computed lazily inside the gem only when it's `nil`. The old `<< 'style'` crashed outright on `nil`, and naively assigning `['style']` would have silently replaced the whole safe-list instead of extending it. Fixed by explicitly setting it to `sanitizer.class.allowed_attributes + ActionText::Attachment::ATTRIBUTES + ['style']` — verified the `style` attribute still survives sanitization.
    - Rails 7.1's dependency graph pulled `rack` to `3.2.7`, and Puma `5.2.2` predates Rack 3 support entirely (missing `rack/handler`). Bumped Puma to `8.0.2` rather than pinning Rack backward.
    - `test/helpers/cookie_decrypter.rb` (a hand-rolled system-test helper that manually decrypts the session cookie) referenced `ActionDispatch::Cookies::JsonSerializer`, removed in 7.1. The encrypted cookie jar now always encrypts with `NullSerializer` — the value going in is already a plain JSON string dumped by the outer `:json` `cookies_serializer` — so fixed to decrypt raw and then `ActiveSupport::JSON.decode` it.
    - Verified: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors; eager loading and both asset pipelines clean. One new deprecation warning surfaced (`cache_format_version = 6.1` removed in 7.2) — expected, addressed at the 7.2 step.
  - [x] **7.1 → 7.2 done 2026-09-14.** Rails resolved to `7.2.3`. This step also had to bump Ruby (`3.0.7` → `3.3.9`) mid-step: Rails `~> 7.2.0` resolved to `7.2.3.2`, which requires Ruby >= 3.1 — matches RDJesseeBlog's own path through this exact transition (`3.0.7` → `3.3.12` → `4.0.6`; `4.x` deferred to the Rails 8.x steps here too). Base image moved to `ruby:3.3.9-bullseye`; Ruby 3.3.9 ships a modern RubyGems/Bundler already, so the `gem update --system`/bundler-2.2.6 pin from the Ruby 3.0.7 image is gone.
    - Bumping Ruby changes what "Ruby's own bundled default" *is* for the default-gem pins from the 7.0/7.1 steps: `logger` 1.4.3 → `1.6.0`, `mutex_m` 0.1.1 → `0.2.0`. **The `psych` 3.3.2 pin from the 7.1 step was removed, not re-pinned** — Ruby 3.3.9 already bundles Psych 5.1.2 as its own default, so pinning backward would recreate the exact "already activated X, but Gemfile requires Y" conflict this whole pattern exists to avoid.
    - Removing the `psych` pin brought the alias-parsing problem back, and it turned out to run deeper than `database.yml`: **Webpacker 5.2.1's own bundled default config template** (`lib/install/config/webpacker.yml`, vendored inside the gem — not a file this app can edit) uses `<<: *default` anchors and loads them with plain `YAML.load_file`, which Psych 5 rejects by default (`Psych::AliasesNotEnabled`). Since this app's own YAML files are all trusted (nothing parses external/untrusted YAML), added a small compatibility shim in `config/boot.rb` that makes `YAML.load`/`load_file` allow aliases again app-wide, restoring Psych's pre-4.x default — it has to live in `boot.rb`, not a `config/initializers/*.rb` file, because Webpacker's railtie initializer (which trips over this) runs before initializers load. Removing this shim is one of the things to check off once Webpacker itself is replaced.
    - Rails 7.2's dependency graph resolves `pg` unconstrained too, and the ancient locked `1.2.3` doesn't compile against Ruby 3.3's C API (`rb_cData` undeclared). Bumped to `1.6.3`.
    - `pry` 0.13.1 (via `pry-byebug`) also doesn't run on Ruby 3.3 (`Object#=~` removed). Bumped `pry`/`pry-byebug`/`byebug` together.
    - `minitest-reporters`' loose `< 7` ceiling let bundler resolve `minitest` 6.0.6, which Rails 7.2.3's `test_unit` integration isn't compatible with yet (`ArgumentError` in `line_filtering.rb`). Pinned `minitest` to `~> 5.0`.
    - Two new deprecation warnings surfaced, informational only, not fixed now: `ActiveSupport::ProxyObject` (removed in 8.0) and `User#role`'s keyword-arg `enum` syntax (removed in 8.0 — already tracked as its own checklist item below, not folded into this step). Minitest 5.27 also started warning (not failing) that three pre-existing `test/models/user_test.rb` tests set up state but never actually assert anything — a real, pre-existing test-quality gap, not something this step introduced; left as a finding rather than fixed, since it's outside a version-bump step's scope.
    - Verified: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar); eager loading and both asset pipelines clean.
  - [x] **7.2 → 8.0 done 2026-09-14.** Rails resolved to `8.0.5.1`; no Ruby bump needed (Rails 8.0 requires >= 3.2, already satisfied by 3.3.9). Two of last step's *informational* deprecation warnings became hard breaks here, exactly as flagged:
    - `jbuilder` 2.11.2 does `require "active_support/proxy_object"`, fully removed in 8.0 (`LoadError`, not just a warning anymore). Bumped to `2.15.1`.
    - **`User#role`'s keyword-arg `enum role: [...]` syntax was removed outright, not just deprecated** — Rails 8.0 raises `ArgumentError: wrong number of arguments` on class load, breaking every test that touches a `User` (which is most of the suite, since fixtures load one). This was already a standalone checklist item below ("migrate to positional `enum :role, [...]`"), but since it no longer just warns, it had to move into this step rather than stay deferred. Migrated to `enum :role, %i[guest_1 guest_2 user admin]` — same array, same order, verified `User.roles` still maps `guest_1=0, guest_2=1, user=2, admin=3` exactly as before.
    - Verified: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar); eager loading and both asset pipelines clean.
  - [x] **8.0 → 8.1 done 2026-09-14 — the final targeted version.** Rails resolved to `8.1.3.1` (exactly matching RDJesseeBlog's own final version). No Ruby bump needed this time — Rails 8.1 only requires >= 3.2, already satisfied by `3.3.9`; stayed there rather than following RDJesseeBlog's `4.0.6` by rote, per this doc's own "don't assume RDJesseeBlog's numbers are current" guidance. Ruby 4.x stays an option for later if a real reason to move comes up, not a default to copy.
    - **Confirmed RDJesseeBlog's exact JSON regression reproduces here too, at the exact same Rails version**: JSON 3.0.2 (Rails 8.1's resolved default) broke `ActiveStorage::Blob` metadata deserialization (`ArgumentError: wrong number of arguments` in `JSON.parse`, discovered via `custom_metadata` during a variant-attach test, not session decoding this time, but the identical root cause). Applied RDJesseeBlog's own confirmed fix verbatim: pinned `json` to `~> 2.0`, which resolved to `2.21.2` — the exact version RDJesseeBlog is running in production.
    - **Directly verified the critical ActiveStorage finding from RDJesseeBlog's rollout does not reproduce here**, rather than just trusting the source-level read from the original audit: attached an image to a real `User`, requested a `.variant(resize_to_limit: ...)`, and confirmed it processes successfully and writes a row to `active_storage_variant_records` — the exact table whose missing migration broke every image site-wide on RDJesseeBlog after its Rails 8.1 deploy. This app's migration (`20210305030844_create_active_storage_variant_records.active_storage.rb`) is present and functional.
    - Verified: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar); eager loading and both asset pipelines clean.
- [x] Migrate `User#role`'s `enum role: [...]` keyword-arg syntax to the modern positional `enum :role, [...]` form, explicitly preserving the `guest_1=0, guest_2=1, user=2, admin=3` integer mapping. Done as part of the 7.2 → 8.0 version-climb step (see above) — Rails 8.0 removed the old syntax outright rather than just deprecating it, so it could no longer stay deferred.
- [x] **Replace Webpacker, done 2026-09-14.** Chose Shakapacker (Webpacker's maintained successor) over an import-map/esbuild swap: it's still genuinely webpack, so `app/javascript/application/index.js`'s `require.context`-based dynamic loading (this app's whole hand-rolled `window.utils`/`window.projects` global load-order pattern depends on it, a webpack-only API) keeps working unchanged. Functional parity only — the idiomatic import-map/Turbo/Stimulus rewrite stays Phase 2's job, confirmed explicitly with the user mid-step rather than assumed.
  - Gem `shakapacker ~> 8.0` resolved to `8.4.0`; matching npm `shakapacker@8.4.0` (must match exactly, per `ensure_consistent_versioning: true`). Node bumped `12.19.0 → 24.21.0` (Krypton, current LTS) in both `.nvmrc` and `Dockerfile.dev`. `package-lock.json` removed; consolidated onto yarn only, matching `bin/yarn`'s pre-existing convention.
  - New `config/shakapacker.yml` copied from the gem's own template (chicken-and-egg: `shakapacker:install` needs this file to exist before it can even boot the app to run). `bin/rails shakapacker:install` then pulled in the rest of the npm peer deps (babel, babel-loader, compression/terser/webpack-assets-manifest/webpack-merge/webpack-subresource-integrity plugins) automatically rather than hand-adding each one.
  - Deleted, confirmed dead: `config/webpack/{development,test,production,environment}.js`, `config/webpacker.yml`, `bin/webpack`, `bin/webpack-dev-server`, `.browserslistrc` (redundant with package.json's new `browserslist` key), and a genuinely stale `babel.config.js` left over from the app's original "migrate to rails 6 beta" commit years ago — it required Babel plugins never installed as part of this upgrade, and was silently shadowing Shakapacker's own babel preset declared in `package.json`'s `babel` key (a `babel.config.js`, if present, always wins over `package.json`'s `babel` key).
  - Shakapacker doesn't auto-detect a same-named `application.scss` under `packs/` as its own CSS entry the way Webpacker did; fixed by importing it directly from `app/javascript/packs/application.js` (`require("./application.scss")`) so mini-css-extract-plugin picks it up as part of the same entry. Its style rule is gated behind `css-loader`/`style-loader`/`mini-css-extract-plugin` actually being resolvable npm packages (traced into Shakapacker's `getStyleRule.js`) — added all three as devDependencies; without them the *entire* style rule (sass included) silently no-ops rather than erroring.
  - Cleaned up two generator side-effects: a duplicate `javascript_pack_tag` the installer injected into `<head>` (removed, kept the original deliberate end-of-`<body>` placement) and a duplicate `yarn install` line added to `bin/setup`.
  - **Two genuine version-compatibility bugs found and fixed via manual browser verification, not just the automated suite** (the automated suite uses precompiled test packs, not the live dev-server proxy, so it couldn't have caught either one):
    1. `rack-proxy`'s SSRF-hardening default (refuse a Host-header-derived backend unless `:backend`/`allow_dynamic_backend: true` is explicitly passed) landed **at 1.0.0**, not just 2.0.0 as first suspected from skimming the 2.0.0 changelog — confirmed by reading 1.0.2's own `perform_request` source, which still unconditionally 502s. Shakapacker's `DevServerProxy` sets `env["HTTP_HOST"]` etc. directly but never passes either opt-in, and Shakapacker's gemspec has no upper bound on `rack-proxy (>= 0.6.1)`, so Bundler resolves whatever is newest. Fixed by pinning `gem 'rack-proxy', '~> 0.8.3'` (the last pre-1.0 release, confirmed via its source to fall back to the request's own Host header with no refusal).
    2. Even with that fixed, `/packs/*` still failed — this time `403 Invalid Host header` from webpack-dev-server itself. Root cause: `config/shakapacker.yml`'s `dev_server.allowed_hosts: 'auto'` only allows `localhost`/IPs matching the bound host, but `Shakapacker::DevServerProxy` rewrites the forwarded request's Host header to `dev_server.host` (the `shakapacker` Compose service name) before proxying, which `'auto'` doesn't recognize. Fixed by setting `allowed_hosts: 'all'` — acceptable here since DNS rebinding isn't a real risk in this Docker-only dev setup (the host is our own Compose network alias, not attacker-controlled).
  - Verified with a real browser (not just `bin/rails test`/`test:system`, which both use precompiled packs): booted `docker compose up db app shakapacker`, confirmed `/packs/js/application.js`, `/packs/css/application.css`, and the vendor bundle all return `200` (previously `502`/`403`), watched the landing-page icon-swap animation play, and scrolled the homepage to confirm content/CSS render correctly.
  - **Removed the `config/boot.rb` Psych-aliases compatibility shim added at the 7.1 → 7.2 step (see above)**, now that Webpacker (the thing that actually needed it) is gone. Confirmed empirically rather than assumed either way: Shakapacker's own config loader already passes `aliases: true` explicitly wherever it calls `YAML.load_file` (checked `shakapacker/configuration.rb`/`env.rb` in the installed 8.4.0 gem), and Rails 8.1's own `ActiveSupport::ConfigurationFile` (used for `config/database.yml`'s `<<: *default` anchors) has handled this internally for years. Disabled the shim, re-ran the full suite clean, then deleted it outright.
  - Final verification, same 125-example bar as every other step: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors; eager loading clean; both `assets:precompile` (Sprockets) and Shakapacker's own compile (invoked as part of the same `assets:precompile` task) produced their manifests/assets with no errors, only pre-existing bundle-size warnings.
  - **Gotcha found during a later UX re-check (2026-09-14):** dev and production share the same `public_output_path: packs` (only `test` overrides it, to `packs-test`). Running `RAILS_ENV=production bin/rails assets:precompile` against this same dev checkout — done above purely to verify the production pipeline — overwrote the live `public/packs/manifest.json` with production's content-hashed filenames. Shakapacker's dev-mode `Manifest#data` only recompiles when `compiling? => config.compile? && !dev_server.running?`; with the dev server already running, it just trusts whatever's on disk, so the app kept serving 404s for the (now wrong) hashed filenames until `shakapacker` was restarted, forcing a fresh dev compile that rewrote the manifest correctly. Symptom: pack JS/CSS requests 404 (served as Rails' HTML 404 page, browser refuses to run/apply it as JS/CSS), which silently killed the landing animation and every other pack-dependent script. **Takeaway: after any production-mode `assets:precompile` test run against a dev checkout with the dev server also running, restart the `shakapacker` service before trusting dev behavior again** — this isn't a bug in the app, just an artifact of testing both pipelines against one shared `public/packs` directory outside of separate Docker images.
  - **Also found during the same re-check, unrelated to Webpacker/Shakapacker:** `/posts` renders an empty list for logged-out visitors because all 30 dev-seeded posts have `published: false` (`app/views/posts/index.html.erb` filters to `@posts.where(published: true)` for non-admins) — expected behavior given the seed data, not a bug. Published one (`Post.first.update(published: true, ...)`) to confirm the Trix-rendered content itself displays correctly, which it does.
- [x] **Manually smoke-tested the custom Trix toolbar, done 2026-09-14 — found and fixed one real regression.** Logged in as admin (reset the local dev admin's password via `bin/rails runner` for this — dev DB only) and exercised every custom feature listed in CLAUDE.md directly in a real browser, inspecting the underlying Action Text HTML via the hidden Trix input after each: heading levels (H1–H6 dropdown, applies correctly as a block-level `<h#>`), foreground/background color swatches (produces `style="color: rgb(...)"`, exactly what the sanitizer allow-list exists to preserve), underline (`style="text-decoration:underline"`), and Attach Files (opens a native file picker via a dynamically-created `<input type="file">` — confirmed it fires with no JS error; actually completing a file selection wasn't exercised, since a native OS dialog is outside what browser automation can drive).
  - **Found and fixed:** the horizontal-rule attachment button inserted fine in the live editor, but **crashed with a 500 (`NoMethodError: undefined method 'name' for an instance of ActionText::Attachment`) as soon as the saved post was displayed** — a real, previously-undetected regression, not caught by the automated suite (no test exercises this feature) or by the earlier browser-based Webpacker verification (which didn't create rich-text content). Root cause: this app's own view override `app/views/action_text/attachables/_content_attachment.erb` calls `content_attachment.name` to pick which `content_attachments/_<name>.html.erb` sub-partial to render — but neither `ActionText::Attachment` nor the gem's `ActionText::Attachables::ContentAttachment` (confirmed against the installed 8.1.3.1 source) has ever defined a `#name` method; only `#content_type` (e.g. `"vnd.rubyonrails.horizontal-rule.html"`, set client-side in `trix.js`). Whatever Action Text internals this partial was originally written against must have passed something else into that local (a raw node, an older `ContentAttachment` shape) that did respond to `#name` — impossible to pin down further without the app's pre-upgrade Action Text version. Fixed by deriving the same dispatch name from `#content_type` instead (still available via `ActionText::Attachment`'s `delegate_missing_to :attachable`), which is a stable public attribute rather than a duck-typed method this app was never guaranteed. Verified the fix renders `<hr>` correctly both via `bin/rails runner` and in a real browser, with the color/underline styles from the same document surviving alongside it.
  - Re-verified the `style`-attribute sanitizer allow-list in `config/initializers/action_text.rb` survives — confirmed above, color/underline `style` attributes came back intact on the saved, re-rendered post.
  - Full verification after the fix: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar).
- [x] **Replace `sass-rails`/`sassc`, done 2026-09-14.** Swapped for `dartsass-rails`, the Rails-team-maintained successor (LibSass/`sassc` was deprecated upstream in 2020; Rails 7+ generators no longer offer `sass-rails` at all). Functional parity only — Sprockets still serves the compiled CSS via `stylesheet_link_tag 'application'` exactly as before; this is unrelated to Shakapacker's own separate webpack-side SCSS handling for `app/javascript/packs/application.scss`, already done in the Webpacker → Shakapacker step above.
  - `dartsass-rails` (`0.5.1`) depends on `sass-embedded` (resolved `1.104.1`) — a real Dart Sass binary wrapped in a gem, not a native C-extension build like `sassc`/LibSass was.
  - **Removing `sass-rails` also silently removed `sprockets-rails` entirely** — it was never a direct Rails dependency, only pulled in transitively via `sass-rails`/`sassc-rails` (Rails 7+ genuinely unbundled the asset pipeline). The app still needs Sprockets directly for `app/assets/{images,audio,files}`, so added `gem 'sprockets-rails'` explicitly rather than relying on it being incidental.
  - Ran `bin/rails dartsass:install`, which set the convention-based entry point at `app/assets/stylesheets/application.scss` (compiled to `app/assets/builds/application.css`, gitignored) and pointed `app/assets/config/manifest.js` at `link_tree ../builds` instead of the old `link_directory ../stylesheets .css`.
  - **The old `app/assets/stylesheets/application.css` used Sprockets' `*= require_tree .` / `*= require_self` directives to bundle all 26 per-feature `.scss` partials — Dart Sass has no directory-wide auto-require equivalent, only explicit `@import`/`@use`.** Verified first that none of the 26 files share any Sass state (`grep` for `@import`/`@use`/`@mixin`/`@include`/`$variable` across all of them came back empty — fully independent, standalone files), so a flat alphabetically-ordered `@import` list in the new `application.scss` is an exact functional match for what `require_tree` produced, with the original `body{...}` rule kept last (matching `require_self`'s original position, so it can still override anything above).
  - **Two more Sprockets/dartsass-rails integration conflicts found and fixed, neither documented in dartsass-rails' own install flow:**
    1. `config/initializers/assets.rb` had a leftover `config.assets.precompile += %w[projects_3d_scroll.css]` line asking Sprockets to compile that one file as its own standalone target via the (now-removed) `sassc` processor — dead even before this migration, since nothing ever loaded it as a separate stylesheet (only `stylesheet_link_tag 'application'` is used anywhere). Removed it.
    2. Rails auto-adds every `app/assets/*` subdirectory (including `app/assets/stylesheets`) to Sprockets' own load path regardless of `manifest.js`, and `sprockets-rails` always precompiles a logical `application.css` by convention. With `application.scss` sitting in that load path, Sprockets kept resolving `application.css` back to it and trying to compile it through its own (now absent) Sass processor — `LoadError: cannot load such file -- sassc`, both during `assets:precompile` and live in development. Fixed in the same initializer: `config.assets.precompile.delete('application.css')` plus removing `app/assets/stylesheets` from Sprockets' load path inside a `config.assets.configure` block (has to happen there, not directly in the initializer — `sprockets-rails` adds its own default paths in an initializer that runs *after* `config/initializers/*.rb`, so a plain `config.assets.paths.delete` at the top level gets silently overwritten; `configure` blocks run lazily when the `Sprockets::Environment` is actually built, after every path has already been added). `env.paths` also turned out to be a frozen array with no single-path removal method, so rebuilt it via `clear_paths` + `append_path`.
  - Verified with a real browser (not just the automated suite, which doesn't render CSS): landing page, login modal (blurred backdrop, toggle switch), the slide-out nav menu, and success notifications all render with correct styling after logging out and back in.
  - Full verification: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar); eager loading clean; both `assets:precompile` (Sprockets, now producing one `application-<hash>.css` from the Dart Sass build plus Action Text's own `trix-<hash>.css` — no more per-file `background_image-*.css`/`blog-*.css`/etc. stragglers from the old per-file Sprockets Sass pipeline) and `dartsass:build` (dev) ran clean, warnings only (`@import`/`random()` deprecations — Dart Sass 3.0 will remove both, not addressed now since it's a syntax modernization, not a functional-parity concern for this phase).
- [x] **Move `rubocop`, `brakeman`, `pry-byebug`, `faker`, and `humanize` out of the Gemfile's default group, done 2026-09-14.** Checked each one's actual usage (`grep` across `app/`, `lib/`, `test/`, `db/`) before picking a group, rather than assuming all five belonged together:
  - `rubocop`/`brakeman` → `:development`, both with `require: false` (invoked as external CLI commands via `bundle exec`, never `require`d by app code).
  - `humanize` → `:development` only — its one usage is `lib/tasks/create_fixtures_from_db.rake`, a dev-only fixture-generation helper (confirmed via `grep`, not referenced anywhere else).
  - `faker` and `pry-byebug` → **`:development, :test`, not `:development` alone** — `faker` turned out to also be used by `test/helpers/generate_user_info.rb`, not just `db/seeds.rb`, so `:development`-only would have broken that test helper. Grouped `pry-byebug` alongside it for the same reason debugging tools usually go in both (dropping a breakpoint inside a failing test is exactly as common as inside a dev-server request).
  - Verified: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar — confirms `faker` still loads under `:test`); eager loading clean; `bundle exec rubocop`/`bundle exec brakeman` both still run; `bin/rails runner -e production` boots cleanly (confirms none of these five are accidentally needed at runtime). No `Gemfile.lock` changes needed — regrouping is a `Bundler.require` concern, not a dependency-resolution one.
- [x] **Update Puma, Bundler, Node LTS, and remaining gems, done 2026-09-14.** Puma (`8.0.2`) and Node (`24.21.0`, Krypton) were already current from earlier steps this session. Checked every other gem's actual latest version before touching anything, rather than blindly bumping:
  - `pg`, `jbuilder`, `bootsnap` were already at latest (`1.6.3`, `2.15.1`, `1.26.0`) — no change needed, just re-confirmed via `bundle lock --update`.
  - `bcrypt` `3.1.16 → 3.1.22`, `diffy` `3.4.0 → 3.4.4` — routine bumps, no issues.
  - `aws-sdk-s3` `1.89.0 → 1.232.0` — a large-looking gap that's normal for this gem (near-weekly releases); not exercised against real S3 in this dev/test environment (development/test both use local/`:test` storage — see the earlier `active_storage.service` finding), so this is verified structurally (boots, resolves) but not against production S3 traffic.
  - Bundler `2.5.22 → 2.7.2` (latest 2.x, not the 4.x line — Bundler skipped 3.x entirely, and there's no reason found yet to chase a new major, matching this app's own Ruby 4.x stance). Bundler isn't a Gemfile-declared runtime dependency, so this doesn't hit the same "already activated X" class of conflict as the logger/mutex_m/psych default-gem pins — just installed the newer version directly. Had to set `ENV BUNDLER_VERSION=2.7.2` in `Dockerfile.dev`, not just `gem install` it: a bare `bundle` command still activates Ruby's own bundled default (2.5.22) even with 2.7.2 also installed, so without forcing it, a from-scratch image rebuild would silently revert `Gemfile.lock`'s `BUNDLED WITH` line back to 2.5.22 every time. Also fixed a real Bundler 2.7+ deprecation this bump surfaced: `platforms: %i[mri mingw x64_mingw]` (on `pry-byebug`) is deprecated in favor of the consolidated `:windows` platform.
  - Added explicit Linux platforms to the lockfile (`bundle lock --add-platform x86_64-linux --add-platform aarch64-linux`) — `aarch64-linux` matches this Docker dev environment (Apple Silicon host), `x86_64-linux` matches Heroku's actual dyno architecture, so the lockfile resolves deterministically on both without needing a fresh per-machine `bundle install`.
  - **`mini_magick` `4.11.0 → 5.4.0` and `image_processing` `1.12.1 → 2.1.0` together surfaced a real, multi-layered boot-breaking regression** — the most involved finding of this step:
    1. `image_processing` 2.x's whole redesign is "bring your own backend": it dropped its hard gemspec dependency on `ruby-vips` that 1.x used to pull in transitively (confirmed via its actual gemspec — zero runtime dependencies at all now). `ActiveStorage::Engine`'s default `config.active_storage.analyzers` list references `ActiveStorage::Analyzer::ImageAnalyzer::Vips` as a bare constant (not lazily), which unconditionally `require`s `image_processing/vips` - without `ruby-vips` installed, this raises a `LoadError` at boot whose wording ("...requires the ruby-vips gem...") doesn't match either pattern `ActiveStorage::Engine`'s own rescue looks for (`/libvips/` or `/image_processing/`), so instead of the intended graceful warning, it crashes boot entirely - a genuine gap in Rails 8.1.3.1's own error handling for this specific image_processing 2.x wording.
    2. Tried the "obvious" fix - adding `ruby-vips` back explicitly - which surfaces a **second**, separate problem: `config.load_defaults 8.1` (in `config/application.rb`) sets Active Storage's `variant_processor` default to `:vips` (a real Rails default since 7.1, not something this app ever deliberately chose - its Gemfile has only ever declared `mini_magick`/`image_processing`). With `ruby-vips` present, `ActiveStorage::Transformers::Vips` gets referenced, which requires `libvips >= 8.13` for its "untrusted content" safety protections - but Debian Bullseye's `libvips42` apt package is `8.10.5`, and Active Storage hard-`raise`s a `RuntimeError` (not rescued at all) rather than degrade gracefully when that check fails. No apt package upgrade path exists on Bullseye without a base-image bump (out of scope here).
    3. **Fixed by explicitly setting `config.active_storage.variant_processor = :mini_magick`** in `config/application.rb`, restoring the tool this app has actually always used and been built around, rather than the newer, silently-inherited `:vips` default. This sidesteps both problems at once: `ruby-vips` was removed again (never actually needed), and `ActiveStorage::Transformers::Vips` is never referenced since `variant_processor` no longer selects it.
  - Verified variant processing itself still works correctly end-to-end with the new mini_magick/image_processing versions: attached a real image to a `User`, requested `.variant(resize_to_limit: [50, 50]).processed`, confirmed it processes successfully.
  - Full verification: `bin/rails test` 88/159, `test:system` 37/749, both 0 failures/errors (same 125-example bar, survived across three full container rebuilds while chasing the above); eager loading clean; both `assets:precompile` and `dartsass:build` ran clean; a fresh `docker compose build --no-cache` reproduces the exact same `Gemfile.lock` (`BUNDLED WITH 2.7.2`, both Linux platforms) with no manual patching needed.

### 3. Security and application behavior

- [x] **Fix `UsersController#remove_resume`'s missing authorization independently of the rest of this list** — add it to the same `logged_in_user`/`correct_user` `before_action :only` lists as `remove_image`. This doesn't need to wait for anything else here. Fixed pre-upgrade (commit `ab6dffb`); regression test added in Testing strategy Step 2.
- [ ] Convert `remove_image`, `remove_resume`, and `demote_guest` from state-changing GET routes to `DELETE`/`PATCH`, updating links/tests accordingly.
- [x] Fix `CommentsController#create` trusting a client-supplied `user_id` for logged-in users; derive it from the session the same way the guest path derives `guest_user.id`. Fixed and tested in Testing strategy Step 2.
- [ ] Decide on the two orphaned `versions`/`version_associations` tables (drop via migration, or document as intentionally orphaned).
- [x] **Guard `User#guess_city`/`guess_region`/`guess_country`/`guess_address` against a nil `Geocoder.search` result, done 2026-09-14.** Consolidated all four into a single memoized private `geocoder_result` helper (`return nil if ip_address.blank?`, then `Geocoder.search(ip_address).first`) and changed each public method to `geocoder_result&.city` etc. Also fixes a real, separate inefficiency: `app/views/users/_profile.html.erb` calls all three of `guess_city`/`guess_region`/`guess_country` on every own-profile view, which previously meant three separate `Geocoder.search` calls (three live outbound lookups) per page load — now one. Added regression tests in `test/models/user_test.rb` for both trigger conditions: a nil `ip_address`, and a non-nil but ungeocodable one (stubbed via `Geocoder::Lookup::Test.add_stub('0.0.0.0', [])` — the global default stub in `config/environments/test.rb` always returns a result, so this needed its own per-test override to actually exercise the empty-result branch).
- [x] **Fix the `TagsController#destroy` redundant-destroy-in-a-loop bug, done 2026-09-14.** `@tag.destroy` was called *inside* `@posts.each`, not just redundantly (calling destroy repeatedly on an already-destroyed record) but with a real functional consequence: a tag with **zero** posts has an empty `@posts`, so the loop body — and therefore `@tag.destroy` — never ran at all, meaning such a tag could never actually be deleted via this action (despite the success flash message claiming it was). Fixed by moving `@tag.destroy` after the loop, called exactly once regardless of how many posts the tag had. Added regression tests in `test/controllers/tags_controller_test.rb` covering both the exact bug (a tag with no posts now actually gets destroyed) and the original multi-post case (tag removed from every post it was tagged on). Confirmed the tag-cloud UI itself can't surface a zero-post tag to click delete on anyway (`Tag.counts` inner-joins through `taggings`, so untagged tags are invisible there) — a separate, pre-existing UI gap, not something this fix needed to touch.
  - Verified: `bin/rails test` 92/175 (bar moved from 88/159 with the 4 new regression tests), `test:system` 37/749, both 0 failures/errors; eager loading clean.
- [ ] Reconcile `config/database.yml`'s production block (explicit `username`/`password` fields sourced from a `BIG_DUMB_WEB_DEV_DATABASE_PASSWORD` env var that doesn't currently exist on Heroku) against `DATABASE_URL`, which the addon actually sets — confirm which one the app is really connecting with today before changing either.
- [ ] Verify the actual SendGrid configuration and Postgres/S3 credentials rather than assuming a free attached add-on proves mail delivery works.
- [ ] Add a health endpoint, unconditional stdout logging (currently gated on `RAILS_LOG_TO_STDOUT` being present rather than always on), and size Puma threads/database pool within the 20-connection Essential-0 limit.
- [ ] Decide whether to add a CSP now that the CDN-loaded Prism dependency and vendored Font Awesome bundle are both known quantities.

### 4. Docker and repeatable builds

- [ ] Add a multi-stage production Dockerfile, `.dockerignore`, non-root runtime — none exist today. The existing `Procfile` (`web: bundle exec puma -C config/puma.rb`) is already correct and can be reused as-is for the eventual buildpack deploy.
- [ ] Add development Compose with isolated Postgres, local media (see the `config.active_storage.service` decision above), and non-delivering mail. Stub `User#fetch_ip`'s network call in this environment.
- [ ] Precompile assets in the build stage without production database access or secrets.
- [ ] Test clean image builds, boot, migrations, health, and restart persistence on the target architecture (this app has no ffmpeg/audio-processing requirement, so its production image should be noticeably simpler than RDJesseeBlog's).
- [ ] Add CI for tests, production asset build, Docker smoke tests, and dependency/security scanning (Brakeman/bundler-audit/npm audit), reusing RDJesseeBlog's `bin/security-scan`/`bin/container-security-scan` pattern rather than reinventing it.

### 5. Stage, deploy and operate

- [ ] Recheck Heroku stack support — this app is still on `heroku-20`; `heroku-22/24/26` are all available (verified via `heroku stack`). Move to the newest supported stack incrementally, not simultaneously with the Rails version jump.
- [ ] Add the `heroku/nodejs` buildpack explicitly before `heroku/ruby` once Webpacker (and its auto-provisioned Node) is removed — otherwise the build will silently lose its Node runtime.
- [ ] Prepare isolated staging with non-production database/media/email; agree on costs before provisioning temporary paid resources.
- [ ] Require passing tests (with the new coverage from Phase 1), reviewed security findings, successful production asset/Docker builds, and mobile/browser checks covering the 3D portfolio, animated landing page, and blog autosave specifically.
- [ ] Prepare fresh verified backups and a rollback procedure; deploy one site at a time and verify the actually-running release/stack/TLS/media/mail/logs, matching how RDJesseeBlog was rolled out.
- [ ] Set up scheduled database exports, a periodic restore drill, and uptime/error monitoring — none exist today beyond Heroku's continuous-protection reporting.
- [ ] Decide, once deploy cadence and trust in the migration review process are established, whether to keep `db:migrate` a manual `heroku run` step (RDJesseeBlog's current choice, made deliberately to keep a human review point before traffic shifts) or automate it via a Heroku release phase.

## Cost plan

Verified via `heroku addons`/`heroku apps:info`: `heroku-postgresql:essential-0` (~$5/month cap) plus one Basic dyno (~$7/month) — about $12/month base, matching the September 9 assessment. `pointdns:developer` and `sendgrid:starter` both report free. RDJesseeBlog adds another ~$12/month on the same account. Recent account invoices total ~$39/month; the remaining ~$15 has not been attributed to a specific line item — inspect the billing breakdown before proposing removals, and preserve PointDNS if it serves active DNS records.

Docker on Heroku is for portability and repeatability, not direct savings. Keep the small database plan unless measured usage requires more.

## Definition of done

- Supported current Ruby/Rails/dependencies with any exception documented (expect at least a JSON-version exception, mirroring RDJesseeBlog's).
- The `remove_resume` authorization gap and the `CommentsController` trusted-`user_id` issue fixed, whether as part of this upgrade or independently before it.
- Latest validated Heroku stack actually deployed, not merely selected for the next build.
- Reproducible Docker setup and clear local/build/deploy documentation.
- The Testing strategy section's Step 3 completion bar actually met before the version climb started, not retrofitted afterward: `PostsController`/`CommentsController` have real tests, the five empty model stubs have real assertions, zero test files remain fully commented-out, and system tests run headlessly in CI/Docker.
- Verified recovery path, scheduled backups, and basic monitoring.
- Documented app-level recurring costs and the remaining account-level billing discrepancy.

## References

- [Rails releases](https://rubyonrails.org/) and [upgrade guide](https://guides.rubyonrails.org/upgrading_ruby_on_rails.html)
- [Ruby releases](https://www.ruby-lang.org/en/downloads/)
- [Heroku stacks](https://devcenter.heroku.com/articles/stack), [Ruby support](https://devcenter.heroku.com/articles/ruby-support-reference), and [pricing](https://www.heroku.com/pricing/)

Version and pricing observations are dated; recheck before implementation or purchasing resources.
