<div id="all-posts-container">
  <% if @posts.any? %>
    <% @posts.each do |post| %>
    <% @post = post %>
      <%= will_paginate @posts %>
      <%= render 'show', post: post, show_all: true %>
    <% end %>
  <% end %>
</div>