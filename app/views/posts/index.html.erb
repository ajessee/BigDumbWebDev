<% provide(:title, "Blog") %>
<div id="all-posts-container">
  <% if @posts.any? %>
    <% if logged_in? && current_user.admin? %>
      <% @posts.each do |post| %>
        <% @post = post %>
          <% if mobile_device == "mobile" %>
            <%= will_paginate @posts, :page_links => false %>
          <% else %>
            <%= will_paginate @posts %>
          <% end %> 
        <%= render 'show', post: post, show_all: true %>
      <% end %>
    <% else %>
        <%= render 'show_user', posts: @posts %>
    <% end %>
  <% end %>
</div>